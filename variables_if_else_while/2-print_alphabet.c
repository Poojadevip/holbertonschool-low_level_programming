#include <stdio.h>
#include <stdlib.h>
/**
*main - prints the alphabet in lowercase, followed by a new line
*
*Return: 0
*/


int main(void)
{
	char c;

	c = 'a;

	while (c <= 'z')
	{
		putchar(c);
		C++;
	}
	putchar('\n');

	return (0);
}
